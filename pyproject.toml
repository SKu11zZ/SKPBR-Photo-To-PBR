[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "skpbr"
version = "0.1.0"
description = "A research-preview, prompt-conditioned BaseColor calibrator for controlled single-image PBR reconstruction."
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [{ name = "Moore \"Zz11uKS\" Ji" }]
keywords = ["pbr", "material-reconstruction", "basecolor", "blender", "computer-vision"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Topic :: Multimedia :: Graphics :: 3D Modeling",
]
dependencies = [
  "numpy>=1.24",
  "Pillow>=10",
  "torch>=2.2",
]

[project.urls]
Homepage = "https://github.com/SKu11zZ/SKPBR-Photo-To-PBR"
Repository = "https://github.com/SKu11zZ/SKPBR-Photo-To-PBR.git"
Issues = "https://github.com/SKu11zZ/SKPBR-Photo-To-PBR/issues"

[project.scripts]
skpbr = "skpbr.cli:main"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
skpbr = ["weights/*.pt"]
